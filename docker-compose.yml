version: '3.8'

services:
  finance-api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=file:./dev.db
      - JWT_SECRET=your_super_secret_key
      - PORT=5000
    volumes:
      - ./prisma/dev.db:/app/prisma/dev.db
    restart: always
